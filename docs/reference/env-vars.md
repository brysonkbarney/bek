# Environment Variables

This page separates variables consumed by the current local product from variables reserved for provider-backed integrations.

## Current Variables

| Variable                          | Used by         | Default                  | Required                              | Notes                                                                                                                                                                                                                                                          |
| --------------------------------- | --------------- | ------------------------ | ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `BEK_API_PORT`                    | `apps/api`      | `4317`                   | No                                    | API listen port for `pnpm dev:api` and `pnpm dev`.                                                                                                                                                                                                             |
| `BEK_WEB_PORT`                    | `apps/web`      | `5173`                   | No                                    | Vite dev server port.                                                                                                                                                                                                                                          |
| `BEK_PUBLIC_URL`                  | `apps/api`      | Request origin fallback  | No                                    | Public API URL for generated Slack app manifests, tunnels, and deploys. Set this before using `/api/slack/manifest` so Slack callback URLs point at the reachable API.                                                                                         |
| `BEK_WEB_API_URL`                 | `apps/web`      | `http://localhost:4317`  | No                                    | Runtime browser-facing API base URL emitted by the production/static web server as `/bek-config.js`. Change this without rebuilding the web image.                                                                                                             |
| `VITE_BEK_API_URL`                | `apps/web dev`  | `http://localhost:4317`  | No                                    | Local Vite dev fallback and compatibility fallback for the static web server. Published web images should prefer runtime `BEK_WEB_API_URL`.                                                                                                                    |
| `BEK_ADMIN_API_TOKEN`             | `apps/api`      | None                     | Required outside explicit local demo  | Enables bearer-token protection for `/api/*` admin routes. If unset, admin routes fail closed unless `BEK_ALLOW_UNAUTHENTICATED_LOCAL=true` is explicitly set outside production. Production rejects known placeholders and tokens shorter than 32 characters. |
| `VITE_BEK_ADMIN_API_TOKEN`        | `apps/web dev`  | None                     | No                                    | Optional dev-server-only fallback. Production web builds fail if this is set; the web app should prompt for the token at runtime.                                                                                                                              |
| `BEK_ADMIN_PRINCIPAL_ID`          | `apps/api`      | `principal_admin`        | No                                    | Human principal used as the authenticated admin actor for bearer-token and explicit local-bypass admin API calls, including approval decisions.                                                                                                                |
| `BEK_ALLOW_UNAUTHENTICATED_LOCAL` | `apps/api`      | `false`                  | No                                    | Explicit local-only bypass for admin API auth when no token is configured. Ignored in production and when `BEK_REQUIRE_ADMIN_AUTH=true`.                                                                                                                       |
| `BEK_REQUIRE_ADMIN_AUTH`          | `apps/api`      | `false`                  | No                                    | Forces admin auth even outside production.                                                                                                                                                                                                                     |
| `BEK_ADMIN_ORIGINS`               | `apps/api`      | `http://localhost:5173`  | No                                    | Comma-separated CORS allowlist and OAuth return origin for the admin console.                                                                                                                                                                                  |
| `BEK_MAX_REQUEST_BODY_BYTES`      | `apps/api`      | `262144`                 | No                                    | Rejects oversized `/api/*` request bodies before parsing Slack callbacks, admin JSON, or provider webhooks.                                                                                                                                                    |
| `BEK_RATE_LIMIT_MAX_REQUESTS`     | `apps/api`      | `600`                    | No                                    | Per-process `/api/*` request limit per peer/window. Set to `0` only when an upstream limiter is enforced.                                                                                                                                                      |
| `BEK_RATE_LIMIT_WINDOW_MS`        | `apps/api`      | `60000`                  | No                                    | Fixed-window duration for Bek's in-process API rate limiter.                                                                                                                                                                                                   |
| `BEK_TRUST_PROXY_HEADERS`         | `apps/api`      | `false`                  | No                                    | Trust proxy client IP headers only behind an ingress that overwrites `X-Forwarded-For`, `X-Real-IP`, or `CF-Connecting-IP`.                                                                                                                                    |
| `BEK_STORAGE`                     | `apps/api`      | `memory`                 | No                                    | `memory` for zero-config local demos, or `postgres` to load/save the Bek snapshot via `DATABASE_URL`.                                                                                                                                                          |
| `BEK_DB_AUTO_SEED`                | `apps/api/db`   | `true`                   | No                                    | Seeds the default `org_demo` snapshot when Postgres mode starts against an empty migrated database.                                                                                                                                                            |
| `DATABASE_URL`                    | `apps/api/db`   | None                     | For Postgres mode                     | Used by `@bek/db`, migrations, seeding, and the API when `BEK_STORAGE=postgres`; if `BEK_STORAGE` is unset, a `DATABASE_URL` selects Postgres mode.                                                                                                            |
| `BEK_DB_INTEGRATION_DATABASE_URL` | `@bek/db tests` | None                     | No                                    | Optional Postgres URL for DB integration tests that exercise row-level worker claim, heartbeat, and lease-expiry semantics. Normal tests skip these cases.                                                                                                     |
| `BEK_RUN_ADVANCEMENT`             | `apps/api`      | `inline_stub`            | No                                    | `inline_stub` keeps the old synchronous local stub behavior. `worker_local` enqueues API/Slack-created runs into the in-process local worker.                                                                                                                  |
| `BEK_WORKER_QUEUE_BACKEND`        | `apps/api/db`   | Storage-aware            | No                                    | `memory` for zero-config demos, or `postgres` for restart-safe worker records/dead letters/events when `BEK_STORAGE=postgres`.                                                                                                                                 |
| `BEK_SANDBOX_PROVIDER`            | `apps/api`      | None                     | No                                    | Unset, `none`, or `noop` uses deterministic stubs. `docker-local` or `docker` wires the local worker's `opencode-sandbox` runtime through `DockerSandboxProvider`. Compose defaults to `none`.                                                                 |
| `BEK_CREDENTIAL_MASTER_KEY`       | `apps/api`      | None                     | For Slack OAuth token persistence     | 32-byte key, such as `hex:...`, used for the local encrypted credential vault. Losing it means stored OAuth bot tokens cannot be decrypted.                                                                                                                    |
| `BEK_CREDENTIAL_KEY_ID`           | `apps/api`      | `bek-local-vault-v1`     | No                                    | Key identifier written into local vault envelopes. Use a new ID when rotating the local vault key.                                                                                                                                                             |
| `SLACK_SIGNING_SECRET`            | `apps/api`      | None                     | Yes outside tests/local unsigned mode | Used to verify Slack signatures for events, slash commands, and interactivity callbacks.                                                                                                                                                                       |
| `BEK_DEV_UNSIGNED_SLACK`          | `apps/api`      | `false`                  | No                                    | Set to `true` only for local webhook testing without Slack signatures. Never enable in shared or production deployments.                                                                                                                                       |
| `NODE_ENV`                        | `apps/api`      | Process default          | No                                    | `production` never allows unsigned Slack events.                                                                                                                                                                                                               |
| `SLACK_CLIENT_ID`                 | `apps/api`      | None                     | For Slack install redirect            | Used by `GET /api/slack/install` to build Slack OAuth URLs.                                                                                                                                                                                                    |
| `SLACK_CLIENT_SECRET`             | `apps/api`      | None                     | For OAuth callback/exchange           | Required before the OAuth callback accepts a code.                                                                                                                                                                                                             |
| `SLACK_STATE_SECRET`              | `apps/api`      | None                     | For Slack install redirect/callback   | Signs and verifies time-bounded OAuth state.                                                                                                                                                                                                                   |
| `SLACK_REDIRECT_URI`              | `apps/api`      | None                     | For Slack install redirect/callback   | Must match the Slack app OAuth redirect URL.                                                                                                                                                                                                                   |
| `BEK_SLACK_OAUTH_EXCHANGE`        | `apps/api`      | Unset means prod only    | No                                    | If unset, code exchange is enabled only when `NODE_ENV=production`. The env templates set `false`; set `true` outside production when testing real OAuth exchange.                                                                                             |
| `BEK_SLACK_BACKGROUND_DRAIN`      | `apps/api`      | `true`                   | No                                    | Set to `false` to prevent Slack callbacks from best-effort draining worker/outbound queues before returning; operators can drain explicitly.                                                                                                                   |
| `SLACK_BOT_SCOPES`                | `apps/api`      | Built-in defaults        | No                                    | Comma-separated bot scopes for the Slack install redirect.                                                                                                                                                                                                     |
| `SLACK_BOT_TOKEN`                 | `apps/api`      | None                     | For Slack Web API posting             | Optional bot token used to post thread replies, approval buttons, approval decisions, and final answers through `chat.postMessage`.                                                                                                                            |
| `BEK_ALLOW_LEGACY_SLACK_USER_MAP` | `apps/api`      | local bypass only        | No                                    | Allows legacy global Slack user mapping keys such as `{"U123":"principal_bryson"}` for callbacks that include a Slack team ID. Keep `false` for shared/hosted installs.                                                                                        |
| `BEK_SLACK_USER_PRINCIPAL_MAP`    | `apps/api`      | None                     | For local Slack ingress decisions     | JSON object mapping Slack users to Bek principal IDs. Prefer team-scoped keys such as `{"T123:U123":"principal_bryson","T123:U_APPROVER":"principal_admin"}`.                                                                                                  |
| `BEK_GITHUB_EXECUTION`            | `apps/api`      | `disabled`               | No                                    | Controls worker-planned GitHub execution: `disabled`, `fake`, or `real`. Real mode validates GitHub App config at readiness time and performs network calls only after a hash-bound `github.pr` approval.                                                      |
| `GITHUB_API_BASE_URL`             | `apps/api`      | `https://api.github.com` | No                                    | Optional REST API base URL for GitHub Enterprise or tests.                                                                                                                                                                                                     |
| `GITHUB_APP_WEBHOOK_SECRET`       | `apps/api`      | None                     | For GitHub setup preview/webhooks     | Preferred GitHub App webhook secret env var. `GITHUB_WEBHOOK_SECRET` is accepted as a deprecated alias.                                                                                                                                                        |
| `GITHUB_APP_INSTALLATION_ID`      | `apps/api`      | None                     | For installation-token previews       | Default GitHub App installation ID used by `/api/setup/github` when no `installationId` query parameter is supplied.                                                                                                                                           |
| `BEK_SANDBOX_EGRESS_ALLOWLIST`    | `@bek/sandbox`  | None                     | No                                    | Comma-separated host allowlist recorded in Docker sandbox metadata. Enforcement still depends on the configured network/proxy boundary.                                                                                                                        |

## Credential Requirement Matrix

| Scenario                           | Minimum variables                                                                                                                                                        | Notes                                                                                                                                                                                                                  |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Local seeded demo                  | None                                                                                                                                                                     | Uses defaults and in-memory data.                                                                                                                                                                                      |
| Admin-authenticated API            | `BEK_ADMIN_API_TOKEN`, `BEK_REQUIRE_ADMIN_AUTH=true`, optional runtime entry in the web console                                                                          | Required outside explicit local demo mode. `VITE_BEK_ADMIN_API_TOKEN` is dev-server-only; production web builds reject embedded admin tokens.                                                                          |
| Signed Slack callbacks             | `SLACK_SIGNING_SECRET`                                                                                                                                                   | Required for events, commands, and interactivity.                                                                                                                                                                      |
| Slack OAuth install redirect       | `SLACK_CLIENT_ID`, `SLACK_STATE_SECRET`, `SLACK_REDIRECT_URI`                                                                                                            | Builds signed Slack authorize URLs for the raw endpoint and the web Slack install action.                                                                                                                              |
| Slack app manifest                 | `BEK_PUBLIC_URL`, optional `SLACK_REDIRECT_URI`, optional `SLACK_BOT_SCOPES`                                                                                             | Generates the Slack manifest for Events API, interactivity, `/bek`, OAuth redirect, bot events, and scopes. If `BEK_PUBLIC_URL` is unset, Bek falls back to the request origin.                                        |
| Slack OAuth callback/exchange      | `SLACK_CLIENT_ID`, `SLACK_CLIENT_SECRET`, `SLACK_STATE_SECRET`, `SLACK_REDIRECT_URI`, `BEK_CREDENTIAL_MASTER_KEY`, `BEK_SLACK_OAUTH_EXCHANGE=true` outside production    | Code exchange stores bot tokens in the local encrypted vault. If exchange is disabled, callbacks validate state and return without storing a token. Hosted installs still need managed KMS/broker custody.             |
| Slack Web API posting              | Stored OAuth token or `SLACK_BOT_TOKEN`                                                                                                                                  | Self-hosted/local posting. `SLACK_BOT_TOKEN` is a manual fallback when OAuth token persistence is not used.                                                                                                            |
| Durable self-hosted worker         | `BEK_STORAGE=postgres`, `DATABASE_URL`, `BEK_RUN_ADVANCEMENT=worker_local`, `BEK_WORKER_QUEUE_BACKEND=postgres`                                                          | Persists worker records, leases, dead letters, and worker events. Run `pnpm db:migrate` before boot.                                                                                                                   |
| GitHub App validation and webhooks | `GITHUB_APP_ID`, `GITHUB_APP_PRIVATE_KEY`, `GITHUB_APP_WEBHOOK_SECRET`, optional `GITHUB_APP_INSTALLATION_ID`                                                            | Validation helpers, setup preview, signed `/api/github/webhooks` ingress, and package-level installation-token/REST client primitives exist. `GITHUB_WEBHOOK_SECRET` is a deprecated alias.                            |
| Approved GitHub worker execution   | `BEK_GITHUB_EXECUTION=fake` for local no-network validation, or `BEK_GITHUB_EXECUTION=real` plus `GITHUB_APP_ID`, `GITHUB_APP_PRIVATE_KEY`, `GITHUB_APP_INSTALLATION_ID` | Disabled by default. Fake mode exercises the approved worker path locally. Real mode uses the GitHub App token provider and REST client after a hash-bound `github.pr` approval.                                       |
| Model provider calls               | `BEK_MODEL_GATEWAY=vercel_ai_sdk`, `AI_GATEWAY_API_KEY` or `VERCEL_OIDC_TOKEN`, optional `BEK_AI_GATEWAY_TAGS`, optional model registry/benchmark JSON or path vars      | Active for the AI SDK Gateway runtime. Default `BEK_MODEL_GATEWAY=local` keeps zero-config demos deterministic. Gateway routes require benchmark pricing in Bek's model registry.                                      |
| MCP tools                          | `MCP_CONFIG_PATH`, `MCP_GATEWAY_ENCRYPTION_KEY`                                                                                                                          | Reserved until live transport and credential handling are implemented.                                                                                                                                                 |
| Local Docker sandbox provider      | Docker installed on the host, `BEK_RUN_ADVANCEMENT=worker_local`, `BEK_SANDBOX_PROVIDER=docker-local` or `docker`                                                        | Local worker runs the `opencode-sandbox` profile through `DockerSandboxProvider`. This is a sandbox-command adapter, not full OpenCode repo orchestration yet. Compose leaves it off with `BEK_SANDBOX_PROVIDER=none`. |
| Hosted sandbox execution           | `BEK_SANDBOX_PROVIDER`, plus provider-specific credentials such as `E2B_API_KEY` or `VERCEL_OIDC_TOKEN`                                                                  | Reserved until hosted sandbox providers are wired end to end.                                                                                                                                                          |

## Local Docker Compose Variables

These are set inside `docker-compose.yml` for local services. Use `BEK_STORAGE=postgres` to make the API load and save through the Postgres snapshot repository.

| Variable              | Service  | Local value     |
| --------------------- | -------- | --------------- |
| `POSTGRES_USER`       | Postgres | `bek`           |
| `POSTGRES_PASSWORD`   | Postgres | `bek`           |
| `POSTGRES_DB`         | Postgres | `bek`           |
| `MINIO_ROOT_USER`     | MinIO    | `bek`           |
| `MINIO_ROOT_PASSWORD` | MinIO    | `bek-local-dev` |

## Reserved Provider And Persistence Variables

The current source includes a Drizzle schema, model-router foundation, runtime/sandbox contracts, and MCP gateway foundation. These variables are present in `.env.example` so the install shape is stable, but most are not fully consumed by runtime code yet.

| Variable                                                                                                                                                                                                                                                                                                     | Status                     | Purpose                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `DATABASE_URL`                                                                                                                                                                                                                                                                                               | Active for Postgres mode   | Persist orgs, channels, bundles, runs, approvals, and audit events through the snapshot repository.                                                                                                                                                                                                                                                                                   |
| `BEK_RUN_ADVANCEMENT`                                                                                                                                                                                                                                                                                        | Active for local worker    | `worker_local` enables in-process worker advancement. Hosted deployments still need a durable queue/worker backend before paid beta.                                                                                                                                                                                                                                                  |
| `BEK_WORKER_QUEUE_BACKEND`                                                                                                                                                                                                                                                                                   | Active for worker queue    | `postgres` stores worker queue/dead-letter/event state in Postgres for restart-safe self-hosting; `memory` is for zero-config demos.                                                                                                                                                                                                                                                  |
| `BEK_CREDENTIAL_MASTER_KEY`, `BEK_CREDENTIAL_KEY_ID`                                                                                                                                                                                                                                                         | Active for local vault     | Encrypts/decrypts local OAuth token envelopes. Hosted deployments need managed KMS/secret-manager custody, rotation, revocation, and access audit.                                                                                                                                                                                                                                    |
| `REDIS_URL`                                                                                                                                                                                                                                                                                                  | Reserved                   | Valkey-backed queueing, rate limits, locks, and ephemeral cache. Not used by the current Postgres worker queue backend.                                                                                                                                                                                                                                                               |
| `S3_ENDPOINT`, `S3_BUCKET`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`                                                                                                                                                                                                                                       | Reserved                   | Artifact storage through MinIO or an S3-compatible service.                                                                                                                                                                                                                                                                                                                           |
| `SLACK_BOT_TOKEN`                                                                                                                                                                                                                                                                                            | Active fallback            | Manual bot token fallback for outbound Slack Web API posting when no stored OAuth token is available.                                                                                                                                                                                                                                                                                 |
| `AI_GATEWAY_API_KEY`, `VERCEL_OIDC_TOKEN`, `BEK_MODEL_GATEWAY`, `BEK_AI_GATEWAY_TAGS`, `BEK_AI_GATEWAY_MAX_RETRIES`, `BEK_AI_GATEWAY_TIMEOUT_MS`, `BEK_MODEL_ROUTING_MODE`, `BEK_MODEL_PROVIDER_REGISTRY_JSON`, `BEK_MODEL_PROVIDER_REGISTRY_PATH`, `BEK_MODEL_BENCHMARKS_JSON`, `BEK_MODEL_BENCHMARKS_PATH` | Active for Gateway routing | `BEK_MODEL_GATEWAY=vercel_ai_sdk` selects the AI SDK Gateway runtime. Use exactly one primary auth path: `AI_GATEWAY_API_KEY` or `VERCEL_OIDC_TOKEN`. If both are set, the AI SDK uses its documented precedence. Gateway runs emit `model.completed` events with local usage and cost estimates. Budget-enforced routes fail closed unless every policy model has benchmark pricing. |
| `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `OPENROUTER_API_KEY`, `OPENAI_COMPATIBLE_*`, `LITELLM_BASE_URL`                                                                                                                                                                                                       | Reserved for model routing | Reserved for direct provider adapters or private gateways. Provider credentials should live behind a credential broker/model gateway before production.                                                                                                                                                                                                                               |
| `BEK_GITHUB_EXECUTION`, `GITHUB_API_BASE_URL`, `GITHUB_APP_*`, `GITHUB_WEBHOOK_SECRET`                                                                                                                                                                                                                       | Partially active           | GitHub App config validation, setup preview, signed webhook ingress, local workflow contracts, fake approved worker execution, and opt-in real REST execution for hash-bound draft PR workflows. Prefer `GITHUB_APP_WEBHOOK_SECRET`; `GITHUB_WEBHOOK_SECRET` remains a deprecated alias.                                                                                              |
| `MCP_CONFIG_PATH`, `MCP_GATEWAY_ENCRYPTION_KEY`                                                                                                                                                                                                                                                              | Reserved for MCP           | Tool registry/proxy configuration and encrypted tool credentials.                                                                                                                                                                                                                                                                                                                     |
| `BEK_RUNTIME_PROVIDER`, `BEK_SANDBOX_PROVIDER`, `BEK_SANDBOX_EGRESS_ALLOWLIST`, `E2B_API_KEY`, `OPENCODE_BIN`                                                                                                                                                                                                | Partially active           | Unset, `none`, or `noop` keeps sandbox execution on deterministic stubs. `BEK_SANDBOX_PROVIDER=docker-local` or `docker` is active for the local worker sandbox-command adapter. Hosted sandbox providers and real OpenCode execution are still in progress. Runtime sandboxes should receive delegated capabilities, not long-lived provider keys.                                   |
| `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_SERVICE_NAME`                                                                                                                                                                                                                                                           | Reserved for observability | Traces, metrics, and log correlation for hosted/self-hosted operations.                                                                                                                                                                                                                                                                                                               |

## Model Execution Env Notes

Provider/model strings such as `openai/gpt-5.4` and
`anthropic/claude-sonnet-4.8` are stored in Bek model policies. They match the
Vercel AI Gateway and AI SDK string shape. The deterministic local runtime is
still the default; live Gateway execution requires
`BEK_MODEL_GATEWAY=vercel_ai_sdk`.

Bek ships built-in benchmark pricing for the seed models
`openai/gpt-5.4`, `openai/gpt-5.5`,
`anthropic/claude-sonnet-4.8`, and `openai-compatible/local`. For other
Gateway/private models, set exactly one of:

- `BEK_MODEL_PROVIDER_REGISTRY_JSON` or `BEK_MODEL_PROVIDER_REGISTRY_PATH` for a
  complete provider registry.
- `BEK_MODEL_BENCHMARKS_JSON` or `BEK_MODEL_BENCHMARKS_PATH` for benchmark
  pricing overlays on top of the built-in registry.

`/api/setup/status` reports `modelPricingReady`, `missingPricedModels`, and
`modelPricingError` so operators can verify the worker will not fail closed
before trying a live Gateway run.

For Gateway execution:

- Prefer `AI_GATEWAY_API_KEY` for local, CI, or non-Vercel deployments.
- Prefer `VERCEL_OIDC_TOKEN` for Vercel deployments or local Vercel-linked
  development where OIDC is available. Local OIDC tokens must be refreshed when
  they expire.
- Do not put provider-specific keys, Gateway keys, or OIDC tokens in prompts,
  run artifacts, sandbox env, screenshots, fixtures, or issue reports.
- Treat `model.completed` events as the source for provider/model, response
  usage, latency, Gateway response ID, estimated cost, and local actual
  estimate. The local actual is calculated from response usage and Bek's pricing
  benchmark; it is not the provider-billed amount.
- Reconcile the durable `model_usage` ledger against Gateway/provider dashboards
  as a separate process. In Postgres mode, `/api/model-usage` totals come from
  the durable ledger and return `source: "model_usage"`; in memory mode they
  fall back to run-level fields with `source: "runs"`. Neither source is
  provider-billed evidence until reconciliation is added.

## Secret Handling

- Do not commit `.env` files, Slack secrets, model provider keys, GitHub tokens, or sandbox credentials.
- Do not paste raw secrets into prompts, fixtures, screenshots, logs, issues, or docs examples.
- Prefer a platform secrets manager for hosted or shared deployments.
- Treat Slack payloads, MCP tool descriptions, repository files, and model output as untrusted input.
